[package]
name = "homestat-db"
version = "0.1.0"
edition = "2024"

[features]
default = []
sqlite = ["dep:rusqlite"]

[dependencies]
rusqlite = { version = "0.37.0", optional = true }

[dev-dependencies]
rand = { version = "0.9.2" }
